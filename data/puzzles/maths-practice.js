import {question, number, part} from '../helpers.js';

// Original tasks. See docs/maths-practice-review.md for tier boundaries and
// source calibration. Slots 635–684 were unused in all previous revisions.
const algebra=(prompt,answer,explanation,algebraForm='simplified',variables=['x'])=>part(prompt,answer.replace(/(^|[+-])1(?=[a-z])/g,'$1'),{kind:'algebra',marks:2,equivalentMarks:algebraForm==='equivalent'?0:1,algebraForm,variables,explanation});
const numeric=(prompt,answer,explanation)=>number(prompt,answer,{explanation});
const fraction=(prompt,answer,explanation)=>part(prompt,answer,{kind:'fraction',explanation});
const item=(prompt,hint,...parts)=>({prompt,hint,parts});
const tasks=[
 ['Collect a single letter','beginner','A4',i=>item('Simplify each expression.','Add or subtract the coefficients of terms with the same letter.',
   algebra(`Simplify ${i+2}x + ${i+3}x`,`${2*i+5}x`,`Add the coefficients: ${i+2} + ${i+3} = ${2*i+5}.`),
   algebra(`Simplify ${i+8}x − ${i+2}x`,'6x','Subtract the coefficients; the letter stays the same.'))],
 ['Letters and constants','beginner','A4',i=>item('Collect like terms.','Keep letter terms and constant terms in separate groups.',
   algebra(`Simplify ${i+2}x + 3 + x + ${i+1}`,`${i+3}x+${i+4}`,`There are ${i+3} lots of x and the constants total ${i+4}.`),
   algebra(`Simplify ${i+7}x + 9 − 2x − ${i+2}`,`${i+5}x+${7-i}`,'Subtract letter coefficients separately from the constants.'))],
 ['Substitute positive integers','beginner','A2',i=>item(`Use x = ${i+2}.`,'Replace every x by the given value, then multiply before adding.',
   numeric('Find 3x + 2',3*(i+2)+2,`3 × ${i+2} + 2 = ${3*(i+2)+2}.`),
   numeric('Find 5x − 1',5*(i+2)-1,`5 × ${i+2} − 1 = ${5*(i+2)-1}.`),
   numeric('Find x²',(i+2)**2,`Square means ${i+2} × ${i+2}.`))],
 ['Undo an addition','beginner','A17',i=>item('Solve the equations.','Use the inverse operation on both sides.',
   numeric(`x + ${i+3} = ${2*i+9}. Find x.`,i+6,`Subtract ${i+3} from both sides.`),
   numeric(`y − ${i+4} = ${i+7}. Find y.`,2*i+11,`Add ${i+4} to both sides.`))],
 ['Undo a multiplication','beginner','A17',i=>item('Solve the equations.','Multiplication is undone by division; division is undone by multiplication.',
   numeric(`${i+2}x = ${(i+2)*(i+4)}. Find x.`,i+4,`Divide both sides by ${i+2}.`),
   numeric(`y / ${i+2} = ${i+3}. Find y.`,(i+2)*(i+3),`Multiply both sides by ${i+2}.`))],
 ['Write an expression','beginner','A1/A3',i=>item('Write each expression using x.','Identify the operation and keep the unknown as a letter.',
   algebra(`${i+3} more than x`,`x+${i+3}`,'More than means add.','equivalent'),
   algebra(`${i+2} times x`,`${i+2}x`,'Times means multiply.','equivalent'),
   algebra(`${i+4} less than x`,`x-${i+4}`,'Subtract the stated amount from x.','equivalent'))],
 ['Function machine inputs','beginner','A7',i=>item(`A machine multiplies its input by ${i+2}, then adds ${i+3}.`,'For an output, follow the steps in order. For an input, undo them in reverse order.',
   numeric('Output for input 4',4*(i+2)+i+3,'Multiply the input, then add.'),
   numeric(`Input for output ${(i+2)*6+i+3}`,6,'Subtract the addition, then divide by the multiplier.'),
   algebra('Output for input x',`${i+2}x+${i+3}`,'Use the same two operations on x.','equivalent'))],
 ['Multiply letter terms','beginner','A1/A4',i=>item('Write each product in simplified algebraic notation.','Multiply the numerical factors and write repeated letters using powers.',
   algebra(`${i+2} × x × ${i+3}`,`${(i+2)*(i+3)}x`,'Multiply the two numerical factors.'),
   algebra(`${i+2} × x × x`,`${i+2}x^2`,'Two factors of x give x squared.'))],
 ['Rectangle perimeter expression','beginner','A21',i=>item(`A rectangle has length x cm and width ${i+2} cm.`,'A rectangle has two lengths and two widths.',
   algebra('Write a simplified expression for the perimeter in cm',`2x+${2*(i+2)}`,'Add x twice and the fixed width twice.'),
   numeric('Find the perimeter when x = 10',20+2*(i+2),'Substitute 10 for each length.'))],
 ['Linear sequence expression','beginner','A25',i=>item(`The nth term of a sequence is ${i+2}n + ${i+1}.`,'The first term has n = 1. Substitute the position number into the rule.',
   numeric('First term',2*i+3,'Set n to 1.'),numeric('Fifth term',5*(i+2)+i+1,'Set n to 5.'),numeric('Difference between consecutive terms',i+2,'Increasing n by 1 adds the coefficient of n.'))],
 ['Collect two letters','beginner','A4',i=>item('Simplify the expressions.','Terms with x combine with x terms; y terms combine with y terms.',
   algebra(`${i+2}x + y + 3x + ${i+3}y`,`${i+5}x+${i+4}y`,'Collect each letter separately.','simplified',['x','y']),
   algebra(`${i+5}x + ${i+4}y − 2x − y`,`${i+3}x+${i+3}y`,'Subtract the coefficients for each letter.','simplified',['x','y']))],
 ['Table of a linear rule','beginner','A9',i=>item(`The rule is y = ${i+2}x + ${i+3}. Complete three table entries.`,'At x = 0, the multiplication contributes zero.',
   numeric('y when x = 0',i+3,'The constant is the y value at x = 0.'),
   numeric('y when x = 1',2*i+5,'Add the coefficient and the constant.'),
   numeric('y when x = 3',4*i+9,'Multiply the coefficient by 3, then add the constant.'))],
 ['Expand a single bracket','foundation','A4',i=>item('Expand and simplify.','Multiply every term inside the bracket by the outside number.',
   algebra(`${i+2}(x + ${i+3})`,`${i+2}x+${(i+2)*(i+3)}`,'Distribute the multiplier to both terms.','expanded'),
   algebra(`${i+3}(2x − ${i+1})`,`${2*(i+3)}x-${(i+3)*(i+1)}`,'The subtraction sign stays with the constant term.','expanded'))],
 ['Common numerical factor','foundation','A4',i=>item('Factorise fully.','Find the greatest number that divides both coefficients.',
   algebra(`${2*(i+2)}x + ${3*(i+2)}`,`${i+2}(2x+3)`,'Take out the greatest common numerical factor.','factorised'),
   algebra(`${3*(i+2)}x − ${5*(i+2)}`,`${i+2}(3x-5)`,'Divide both terms by the common factor.','factorised'))],
 ['Common letter factor','foundation','A4',i=>item('Factorise fully.','Look for a letter as well as a number shared by all terms.',
   algebra(`x² + ${i+2}x`,`x(x+${i+2})`,'Both terms contain x.','factorised'),
   algebra(`${i+2}x² + ${3*(i+2)}x`,`${i+2}x(x+3)`,'Take out the numerical factor and x.','factorised'))],
 ['Two-step linear equations','foundation','A17',i=>item('Solve for x.','Undo the addition or subtraction before dividing.',
   numeric(`${i+2}x + 3 = ${(i+2)*5+3}`,5,`Subtract 3, then divide by ${i+2}.`),
   numeric(`4x − ${i+2} = ${4*(i+3)-i-2}`,i+3,`Add ${i+2}, then divide by 4.`))],
 ['Unknown on both sides','foundation','A17',i=>item('Solve for x.','Move the x terms to one side and the constants to the other.',
   numeric(`${i+4}x + 2 = ${i+2}x + ${2*(i+3)+2}`,i+3,`Subtract ${i+2}x and 2 from both sides, then divide by 2.`),
   numeric(`5x − ${i+1} = 2x + ${3*(i+2)-i-1}`,i+2,'Collect x terms to obtain 3x, then divide by 3.'))],
 ['Divide an expression','foundation','A4',i=>item('Simplify each expression.','A fraction bar applies to every term in its numerator.',
   algebra(`(4x + ${4*i+6}) / 2`,`2x+${2*i+3}`,'Divide both numerator terms by 2.'),
   algebra(`(${3*(i+2)}x − ${3*(i+1)}) / 3`,`${i+2}x-${i+1}`,'Divide both numerator terms by 3.'))],
 ['Negative substitution','foundation','A2',i=>item(`Use x = −${i+2}.`,'Put a negative substituted value in brackets before applying a power.',
   numeric('Find x²',(i+2)**2,'A negative number multiplied by itself is positive.'),
   numeric('Find 3x − 4',-3*(i+2)-4,'Multiply the negative value by 3, then subtract 4.'),
   numeric('Find 2 − x',i+4,'Subtracting a negative number is addition.'))],
 ['Rearrange an addition formula','foundation','A5',i=>item(`The formula is y = x + ${i+3}.`,'Perform the inverse operation on both sides of the formula.',
   algebra('Make x the subject. Enter the simplified expression in y.',`y-${i+3}`,`Subtract ${i+3} from both sides.`,'rearranged',['y']),
   numeric('Find x when y = 20',17-i,`Subtract ${i+3} from 20.`))],
 ['Rearrange a multiplication formula','foundation','A5',i=>item(`The formula is y = ${i+2}x.`,'Undo multiplication with division on both sides.',
   algebra('Make x the subject. Enter the simplified expression in y.',`y/${i+2}`,`Divide both sides by ${i+2}.`,'rearranged',['y']),
   numeric(`Find x when y = ${7*(i+2)}`,7,'Divide the given y value by its coefficient.'))],
 ['Equation with a bracket','foundation','A17',i=>item('Solve for x.','You can divide both sides by an outside multiplier before undoing the bracket.',
   numeric(`${i+2}(x + 3) = ${8*(i+2)}`,5,'Divide by the multiplier, then subtract 3.'),
   numeric(`3(x − ${i+1}) = ${3*(i+4)}`,2*i+5,'Divide by 3, then add the subtracted amount.'))],
 ['Expand and collect','foundation','A4',i=>item('Expand and simplify.','Distribute first, then group the resulting like terms.',
   algebra(`${i+2}(x + 2) + 3x`,`${i+5}x+${2*(i+2)}`,'Expand the bracket and combine x terms.','expanded'),
   algebra(`4(x + ${i+1}) − 2(x + 1)`,`2x+${4*i+2}`,'The −2 multiplies both terms in its bracket.','expanded'))],
 ['Product of two brackets','standard','A4',i=>item('Expand and simplify.','Multiply each term in the first bracket by each term in the second.',
   algebra(`(2x + ${i+1})(x + 3)`,`2x^2+${i+7}x+${3*(i+1)}`,'Combine the two middle x terms.','expanded'),
   algebra(`(3x − ${i+1})(2x + 1)`,`6x^2+${1-2*i}x-${i+1}`,'Keep the negative sign when multiplying the second term.','expanded'))],
 ['Quadratic factorisation','standard','A4',i=>item('Factorise fully.','For a monic quadratic, find two numbers with the required sum and product.',
   algebra(`x² + ${2*i+5}x + ${(i+2)*(i+3)}`,`(x+${i+2})(x+${i+3})`,'The constants in the brackets add to the x coefficient and multiply to the constant.','factorised'),
   algebra(`x² − ${(i+2)**2}`,`(x-${i+2})(x+${i+2})`,'Use the difference of two squares.','factorised'))],
 ['Simultaneous equations','standard','A19',i=>item(`Solve 2x + y = ${3*i+10} and x − y = 2.`,'Add the equations to eliminate y.',
   numeric('x',i+4,'Adding gives 3x equal to the sum of the right sides.'),
   numeric('y',i+2,'Substitute x into x − y = 2.'))],
 ['Quadratic roots','standard','A18',i=>item(`Solve x² − ${2*i+5}x + ${(i+2)*(i+3)} = 0.`,'Factorise into two brackets, then set each factor to zero.',
   numeric('Smaller root',i+2,'The smaller factor constant gives the smaller root.'),
   numeric('Larger root',i+3,'Both roots make one bracket zero.'))],
 ['Compose two functions','standard','A7',i=>item(`f(x) = ${i+2}x + 1 and g(x) = x − ${i+1}.`,'The inner function acts first. Substitute its entire expression into the outer function.',
   algebra('Find and simplify f(g(x))',`${i+2}x+${1-(i+2)*(i+1)}`,'Replace x in f by the expression for g.'),
   algebra('Find and simplify g(f(x))',i?`${i+2}x-${i}`:'2x','Replace x in g by the expression for f.'))],
 ['Index laws with two letters','standard','A4',i=>item('Simplify the products.','When multiplying powers of the same base, add their exponents.',
   algebra(`x^${i+2} × x^3`,`x^${i+5}`,'Add the two exponents.'),
   algebra(`(2xy)^2 × ${i+2}x`,`${4*(i+2)}x^3y^2`,'Square the coefficient and both letters, then multiply.','simplified',['x','y']))],
 ['Line through two points','standard','A9',i=>item(`A straight line passes through (1, ${i+4}) and (3, ${3*(i+2)+2}).`,'Find the change in y divided by the change in x, then use either point to find the intercept.',
   numeric('Gradient',i+2,'The change in y is twice the gradient.'),
   algebra('Write y as an expression in x',`${i+2}x+2`,'Substitute a point into y = mx + c to find c.','equivalent'))],
 ['Inverse linear function','standard','A7',i=>item(`f(x) = ${i+2}x − ${i+3}.`,'Reverse the operations in reverse order.',
   algebra('Write the inverse function as a simplified expression in x',`(x+${i+3})/${i+2}`,'Add the constant, then divide by the multiplier.','rearranged'),
   numeric(`Find the input whose output is ${5*(i+2)-i-3}`,5,'Apply the inverse operations to the output.'))],
 ['Complete the square','stretch','A11/A18',i=>item(`Write x² + ${2*(i+2)}x + ${i+1} as (x + a)² + b.`,'Half the x coefficient gives a. Expanding the square reveals the constant correction.',
   numeric('a',i+2,'The middle term of (x + a)² is 2ax.'),
   numeric('b',i+1-(i+2)**2,'Subtract a² from the original constant.'))],
 ['Expand three brackets','stretch','A4',i=>item('Expand and simplify.','Expand two brackets first, then multiply every resulting term by the third bracket.',
   algebra(`(x + 1)(x − 1)(x + ${i+2})`,`x^3+${i+2}x^2-x-${i+2}`,'The first two brackets give x² − 1. Multiply this by the final bracket.','expanded'))],
 ['Differentiate a polynomial','stretch','AS differentiation',i=>item(`Differentiate y = ${i+2}x³ + ${i+1}x² − 4x + 7 with respect to x.`,'For ax^n, multiply a by n and reduce the power by one. A constant differentiates to zero.',
   algebra('dy/dx',`${3*(i+2)}x^2+${2*(i+1)}x-4`,'Apply the power rule to each term separately.'))],
 ['Polynomial remainder','stretch','AS factor theorem',i=>item(`P(x) = x³ − ${i+2}x + ${i+4}.`,'The remainder on division by x − a is P(a).',
   numeric('Remainder when divided by x − 2',8-2*(i+2)+i+4,'Substitute x = 2.'),
   numeric('Remainder when divided by x + 1',-1+i+2+i+4,'Substitute x = −1.'))],
 ['Fraction of a quantity','foundation','N8',i=>item(`A class has ${6*(i+4)} students. One third walk to college.`,'Divide by the denominator to find a unit fraction.',
   numeric('Number who walk',2*(i+4),'Divide the class size by 3.'),
   numeric('Number who do not walk',4*(i+4),'Subtract the walkers from the total.'))],
 ['Percentage discount','foundation','R9',i=>item(`A coat costs £${40+20*i}. It is reduced by 25%.`,'Find a quarter of the original price, then subtract it.',
   numeric('Discount in pounds',10+5*i,'25% is one quarter.'),numeric('Sale price in pounds',30+15*i,'Subtract the discount from the original price.'))],
 ['Share in a ratio','foundation','R5',i=>item(`Share £${5*(i+6)} in the ratio 2 : 3.`,'Add the ratio parts to find how many equal units make the total.',
   numeric('Smaller share in pounds',2*(i+6),'Divide the total by 5 and multiply by 2.'),numeric('Larger share in pounds',3*(i+6),'Divide the total by 5 and multiply by 3.'))],
 ['Scale drawing','foundation','R2',i=>item(`On a map, 1 cm represents 2 km. A route measures ${i+3} cm.`,'Use the same scale factor for every distance.',
   numeric('Actual distance in km',2*(i+3),'Multiply the map distance by 2.'),numeric(`Map distance in cm for ${2*(i+5)} km`,i+5,'Divide the real distance by 2.'))],
 ['Mean from a short list','foundation','S4',i=>item(`The readings are ${i+2}, ${i+4}, ${i+6}, ${i+8}.`,'The mean uses the total divided by the number of readings. The range uses the two extremes.',
   numeric('Mean',i+5,'The total is four times the mean.'),numeric('Range',6,'Subtract the smallest reading from the largest.'))],
 ['Simple probability','foundation','P4',i=>item(`A bag contains ${i+2} red counters and ${i+3} blue counters. One counter is chosen at random.`,'Use favourable outcomes divided by all equally likely outcomes.',
   fraction('Probability of red',`${i+2}/${2*i+5}`,'Divide red counters by the total.'),fraction('Probability of blue',`${i+3}/${2*i+5}`,'Divide blue counters by the total.'))],
 ['Convert metric units','foundation','N16',i=>item('Convert the measurements.','There are 1,000 metres in a kilometre and 100 centimetres in a metre.',
   numeric(`${i+2}.5 km in metres`,(i+2.5)*1000,'Multiply kilometres by 1,000.'),numeric(`${i+3}.2 m in centimetres`,(i+3.2)*100,'Multiply metres by 100.'))],
 ['Area and perimeter','foundation','G16',i=>item(`A rectangle is ${i+3} cm by ${i+5} cm.`,'Area counts square units; perimeter measures the distance around the boundary.',
   numeric('Area in cm²',(i+3)*(i+5),'Multiply length by width.'),numeric('Perimeter in cm',4*i+16,'Add all four sides.'))],
 ['Angles on a straight line','foundation','G3',i=>item(`Two adjacent angles on a straight line are ${35+10*i}° and x°.`,'Angles on a straight line total 180°.',
   numeric('x in degrees',145-10*i,'Subtract the known angle from 180.'),numeric('An angle vertically opposite x, in degrees',145-10*i,'Vertically opposite angles are equal.'))],
 ['Best value comparison','foundation','R1',i=>item(`Pack A contains 4 pens for £${4*(i+1)}. Pack B contains 6 pens for £${6*(i+1)+3}.`,'Compare the price per pen using the same units.',
   numeric('Price per pen in Pack A, in pounds',i+1,'Divide the pack price by 4.'),numeric('Price per pen in Pack B, in pounds',i+1.5,'Divide the pack price by 6.'))],
 ['Speed and distance','foundation','R11',i=>item(`A cyclist travels at a steady ${i+10} km/h for 2 hours.`,'Distance equals speed multiplied by time.',
   numeric('Distance in km',2*(i+10),'Multiply speed by 2 hours.'),numeric(`Time in hours for ${3*(i+10)} km at that speed`,3,'Divide distance by speed.'))],
 ['Rounding and estimation','foundation','N15',i=>item(`Estimate ${21+i} × ${38+i} by rounding each number to the nearest ten.`,'Round each factor first, then multiply the rounded values.',
   numeric('First rounded factor',i===4?30:20,'A units digit of 5 rounds up.'),numeric('Second rounded factor',40,'Each value is closer to 40 than to the neighbouring tens.'),numeric('Estimated product',(i===4?30:20)*40,'Multiply the rounded factors.'))],
 ['Two independent events','standard','P8',i=>item(`A fair ${i+4}-sided die numbered 1–${i+4} is rolled twice.`,'The two rolls are independent, so multiply their probabilities.',
   fraction('Probability of rolling 1 both times',`1/${(i+4)**2}`,'Multiply 1/n by 1/n.'),fraction('Probability of at least one roll not being 1',`${(i+4)**2-1}/${(i+4)**2}`,'Subtract the probability of two ones from 1.'))],
 ['Arithmetic progression total','standard','A25',i=>item(`A row pattern has ${i+3} dots in row 1 and adds 2 dots in each following row. There are 10 rows.`,'Pair the first and last rows: each such pair has the same total.',
   numeric('Dots in row 10',i+21,'There are nine increases of 2.'),numeric('Total dots',10*(i+12),'Five pairs each contain twice the first row plus 18 dots.'))],
 ['Triangle area','foundation','G16',i=>item(`A triangle has base ${2*(i+3)} cm and perpendicular height ${i+4} cm.`,'A triangle has half the area of a rectangle with the same base and perpendicular height.',
   numeric('Area in cm²',(i+3)*(i+4),'Multiply base by height, then divide by 2.'),
   numeric('Area in cm² after doubling only the base',2*(i+3)*(i+4),'Doubling the base doubles the area.'))]
];

export default tasks.map(([title,level,reference,make],index)=>({
  ...question(635+index,'classic maths',title,index<35?'Algebra practice':'Maths practice',[index<35?'maths:algebra':'maths:number',`challenge:${level}`],make),
  challengeLevel:level,estimatedMinutes:5,reviewStatus:'pending',
  mathsTier:['beginner','foundation'].includes(level)?'GCSE Foundation':level==='standard'?'GCSE Higher':'GCSE Higher / introductory AS',
  mathsReference:reference
}));
