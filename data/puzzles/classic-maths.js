import {number as n, part as p} from '../helpers.js';
import more from './classic-maths-more.js';
const source={url:'https://mrbartonmaths.com/puzzles/',note:'Classic puzzle idea from the linked collection; concise adapted wording and independently derived solution.'};
const rows=[
 {slot:135,title:'Every second position',prompt:'Positions 1–100 form a circle. Starting at 1, remove the next remaining position and move to the following survivor. Repeat until one remains.',hint:'Try smaller circles, especially powers of two.',parts:[n('Surviving position',73,{marks:3,explanation:'At 64 positions, position 1 survives. For 100, the extra 36 shift the survivor by 72: 73.'})]},
 {slot:136,title:'Toggling lockers',prompt:'All 1,000 lockers start closed. On pass k, toggle every locker whose number is a multiple of k. Complete passes 1–1,000.',hint:'Count how many divisors each locker number has.',parts:[n('How many lockers finish open?',31,{marks:3,explanation:'Only squares have an odd number of divisors. There are 31 squares up to 1,000.'})]},
 {slot:137,title:'Bridge and torch',prompt:'Four walkers take 1, 2, 7 and 10 minutes to cross. At most two cross together, taking the slower time. Every crossing requires the single torch; returning also takes time.',hint:'Compare using the fastest walker for every return with sending the two slowest together.',parts:[n('Minimum time for everyone to reach the far side, in minutes',17,{marks:3,explanation:'Crossings: 1+2 (2), 1 returns (1), 7+10 (10), 2 returns (2), 1+2 (2). Total 17.'})]},
 {slot:138,title:'A balance and five weights',prompt:'Choose fixed positive integer weights for a two-pan balance. You may place weights on either pan and use each at most once per weighing. Measure every integer mass from 1 to 121.',hint:'Putting a weight on the object’s side subtracts its mass from the opposite side.',parts:[n('Minimum number of weights',5,{explanation:'With n weights, balanced choices represent at most (3^n − 1)/2 positive masses.'}),p('Give one suitable set, separated by commas','1, 3, 9, 27, 81',{kind:'integer-set',accepted:['1,3,9,27,81'],marks:2,explanation:'Balanced ternary covers every mass from 1 to 121 with these weights.'})]},
 {slot:139,title:'Two fragile test objects',prompt:'A building has 100 floors and two identical test objects. An object breaks at or above one unknown threshold floor, but survives and can be reused below it. Find the threshold with the fewest drops in the worst case.',hint:'After a break, only one object remains. Make the gaps between first-object tests progressively smaller.',parts:[n('Minimum worst-case number of drops',14,{marks:3,explanation:'Choose decreasing gaps 14, 13, 12, … . Fourteen drops cover up to 105 floors; thirteen cover only 91.'})]}
];
const sourceIds={135:1,136:13,137:23,138:21,139:29};
export default [...rows.map(row=>({...row,sourceId:sourceIds[row.slot]})),...more.map((row,i)=>({...row,slot:140+i}))].map(({slot,title,sourceId,...v})=>({slot,title,sourceId,focus:'classic maths',format:'Classic reasoning problem',tags:[],setSize:3,estimatedMinutes:10,fixed:true,source,variations:[{...v,parts:v.parts.map((p,i)=>({...p,id:String(i)}))}]}));
