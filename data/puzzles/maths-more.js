import {question as q, number as n} from '../helpers.js';
const tasks=[
 ['Handshake network','foundation',i=>{const k=i+6;return [`Each of ${k} servers connects directly to every other server, once per pair.`,['Connections at each server','Total distinct connections','Extra connections when one new server joins'],[k-1,k*(k-1)/2,k],'Count unordered pairs; each connection has two endpoints.'];}],
 ['Meet again','foundation',i=>{const a=[4,6,8,9,10][i],b=[6,9,12,12,15][i],g=(x,y)=>y?g(y,x%y):x,l=a*b/g(a,b);return [`Two lights flash together at time zero, then every ${a} and ${b} seconds respectively.`,['Seconds until they next flash together','Flashes of the first light after time zero, up to that meeting','Flashes of the second light in that interval'],[l,l/a,l/b],'Find the least common multiple of the intervals.'];}],
 ['Remainder lock','standard',i=>{const x=17+30*i;return [`A lock code is an integer from ${30*i} to ${30*i+29}, inclusive. Dividing it by 3 leaves 2; dividing by 5 leaves 2; dividing by 2 leaves 1.`,['The code','Quotient on division by 5','Quotient on division by 3'],[x,Math.floor(x/5),Math.floor(x/3)],'List numbers in the stated interval that leave remainder 2 when divided by 5. Test that shorter list against the other two remainder conditions.', 'Start with values congruent to 2 modulo 15, then use odd parity.'];}],
 ['Grid routes','standard',i=>{const w=i+2,h=3,choose=(n,k)=>{let x=1;for(let j=1;j<=k;j++)x=x*(n-j+1)/j;return Math.round(x);};return [`Walk from (0,0) to (${w},3), moving only one unit right or up each step.`,['Steps in any route','Number of different routes','Routes whose first step is up'],[w+h,choose(w+h,h),choose(w+h-1,h-1)],'A route is determined by the positions of its up steps.'];}],
 ['Cut a painted cube','standard',i=>{const k=i+3;return [`Paint all six faces of a solid cube, then cut it into ${k} × ${k} × ${k} equal small cubes.`,['Small cubes with three painted faces','Small cubes with exactly two painted faces','Small cubes with no painted faces'],[8,12*(k-2),(k-2)**3],'Separate corners, edge interiors, face interiors and the unpainted centre.'];}],
 ['Pigeonhole guarantee','foundation',i=>{const c=i+3;return [`A box has unlimited tokens in each of ${c} colours. You draw without seeing the colours.`,['Minimum draws guaranteeing two of one colour','Minimum draws guaranteeing three of one colour','Largest draw count that could still have no matching pair'],[c+1,2*c+1,c],'Consider the most evenly spread possible draw before a match becomes unavoidable.'];}],
 ['Weighing capacity','stretch',i=>{const w=i+2;return [`Exactly one coin is lighter than all the others. A balance has three possible outcomes per weighing. There are ${3**w} suspect coins, and adaptive equal-sized groups may be weighed.`,['Minimum weighings in the worst case','Coins in each group for an equal three-way first split','Outcomes possible in one weighing'],[w,3**(w-1),3],'Each weighing identifies one of three groups; compare the number of outcomes with the suspects.'];}],
 ['Missing average','foundation',i=>{const k=i+4,a=i+10,x=i+7,total=k*a;return [`${k} readings have mean ${a}. After one reading is removed, the total of the remaining readings is ${total-x}.`,['Original total','Removed reading','Number of remaining readings'],[total,x,k-1],'Mean multiplied by count gives the total.'];}],
 ['Work together','standard',i=>{const a=2*(i+2),b=3*(i+2),l=6*(i+2);return [`Worker A completes a job in ${a} hours and B in ${b} hours, each at a constant rate. Use ${l} equal job-units for the following answers.`,['Units A completes per hour','Units B completes per hour','Combined units per hour'],[3,2,5],'Convert completion times into rates before adding them.'];}],
 ['Opposite-direction lap','standard',i=>{const track=120*(i+2),a=3,b=2;return [`Two robots start together on a circular track of ${track} metres. One travels clockwise at ${a} m/s, the other anticlockwise at ${b} m/s.`,['Seconds until they first meet again','Metres travelled by the faster robot by then','Seconds between successive meetings'],[track/5,3*track/5,track/5],'Their combined speed closes one complete lap between meetings.'];}],
 ['Binary switches','foundation',i=>{const k=i+4;return [`A panel has ${k} independent switches. Each is either on or off.`,['Possible complete switch patterns','Patterns with the first switch on','Patterns with exactly one switch on'],[2**k,2**(k-1),k],'Multiply independent choices; a fixed switch removes one binary choice.'];}],
 ['Two-digit reversal','standard',i=>{const a=i+3,b=i+1;return [`A two-digit number has digit sum ${a+b}. It is 18 greater than the number obtained by reversing its digits.`,['Tens digit','Units digit','Original number'],[a,b,10*a+b],'The difference between a number and its reversal is nine times the digit difference.'];}],
 ['Rectangles on a grid','stretch',i=>{const w=i+2,h=i+3;return [`A rectangular grid contains ${w} columns and ${h} rows of unit squares. Count rectangles aligned to its grid lines.`,['Number of vertical grid lines','Ways to choose the left and right boundary','Total rectangles'],[w+1,w*(w+1)/2,w*(w+1)*h*(h+1)/4],'Choose two vertical and two horizontal boundary lines independently.'];}],
 ['Tournament elimination','foundation',i=>{const k=2**(i+3);return [`A single-elimination tournament begins with ${k} players. There are no byes, draws or third-place matches.`,['Matches to find one winner','Matches in the first round','Wins made by the champion'],[k-1,k/2,i+3],'Each match eliminates exactly one player; each round halves the remaining field.'];}],
 ['Consecutive integers','standard',i=>{const middle=i*7+12;return [`Five consecutive integers total ${5*middle}.`,['Middle integer','Smallest integer','Sum of the smallest and largest'],[middle,middle-2,2*middle],'For an odd number of consecutive integers, the average is the middle value.'];}],
 ['Inclusion and exclusion','standard',i=>{const both=i+3,a=both+8,b=both+5,total=30+i;return [`Of ${total} students, ${a} use Python, ${b} use JavaScript and ${both} use both.`,['Students using at least one of these languages','Students using neither','Students using exactly one'],[a+b-both,total-a-b+both,a+b-2*both],'Subtract the overlap once for the union and twice for exactly one.'];}],
 ['Clock-hand coincidence','stretch',i=>{const hour=i+1;return [`At ${hour}:00 the hour hand is ${30*hour} degrees ahead of the minute hand. Both move continuously. To avoid rounding, express the requested time as a fraction with denominator 11.`,['Minute-hand speed in degrees per minute','Numerator of minutes until the hands next coincide','Numerator of degrees the minute hand travels before then, over 11'],[6,60*hour,360*hour],'Find how far each hand moves in one minute. Use the difference between their speeds to work out how quickly the initial angular gap closes.', 'The minute hand gains 5.5 degrees per minute on the hour hand.'];}],
 ['Reliable majority','stretch',i=>{const f=i+1,nodes=2*f+1;return [`A decision uses a strict majority of ${nodes} replicas. Assume at most ${f} give an incorrect Boolean answer and every correct replica agrees.`,['Smallest strict majority','Maximum possible incorrect votes','Minimum overlap between any two groups of that majority size'],[f+1,f,1],'Two subsets whose sizes total more than the population must overlap.'];}],
 ['Optimal adjacent merges','stretch',i=>{const a=i+2;return [`Four files have sizes ${a}, ${a}, ${2*a}, ${4*a} MB. Merging any two costs their combined size, and replaces them with that combined file. Find the least possible total merge cost.`,['Cost of the first merge in an optimal strategy','Cost of the second merge in that strategy','Minimum total cost'],[2*a,4*a,14*a],'Compare starting with a large pair and with a small pair. Track how often each original file is processed again in later merges.', 'Merge the two smallest available files each time, counting repeated processing.'];}],
 ['Domino tilings','stretch',i=>{const k=i+4,f=[1,1];for(let j=2;j<=k;j++)f.push(f[j-1]+f[j-2]);return [`Tile a 2 × ${k} board with 2 × 1 dominoes. Rotations are allowed; no overlap or gaps. Reflections count as different layouts if occupied pairs differ.`,['Dominoes needed','Number of tilings','Tilings starting with a vertical domino in the left column'],[k,f[k],f[k-1]],'The left edge contains either one vertical domino or two horizontal dominoes.'];}]
];
export default tasks.map(([title,level,make],j)=>({...q(615+j,'classic maths',title,'Mathematical reasoning',[`challenge:${level}`],i=>{const [prompt,labels,answers,hint,explanation=hint]=make(i);return {prompt,hint,parts:answers.map((a,k)=>n(labels[k],a,{explanation}))};}),challengeLevel:level,estimatedMinutes:8}));
