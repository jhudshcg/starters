import {examQuestion as q, item as p} from './exam-helpers.js';

const pick = (prompt, options, correct) => [prompt, options[correct], [], {options}];
const context = (question, prompts, codes) => ({...question,
  variations: question.variations.map((variation, index) => ({...variation,
    prompt: prompts[index], ...(codes ? {code: codes[index]} : {})}))});

export default [
context(q(126, 'CA1.1', 'Modelling a capacity decision', [
  p({'1.1.14':'a','1.1.15':'a'},
    ['State the additional input needed alongside booked seats to decide whether this request fits.', 'seats requested', ['requested seats','number of seats requested','requested']],
    ['State the additional input needed alongside used storage to decide whether this upload fits.', 'upload size', ['file size','size of upload','size of the upload','size of the file']],
    'The decision needs the size of the proposed addition as well as the capacity already used.'),
  p({'1.1.15':'b'},
    ['State the Boolean result for a request for 4 seats.', 'False', ['reject','rejected']],
    ['State the Boolean result for an upload of 15 MB.', 'True', ['accept','accepted']],
    'Compare the existing use plus the requested amount with the fixed capacity.'),
  p({'1.1.15':'c'},
    ['Which stored quantity must change after an accepted booking?', 'booked seats', ['seats booked','number of booked seats','booked']],
    ['Which stored quantity must change after an accepted upload?', 'used storage', ['storage used','used space','used']],
    'The stored use increases after an accepted request.'),
  p({'1.1.15':'d'},
    ['Which value remains constant while these requests are processed?', '40', ['capacity','seat capacity','40 seats']],
    ['Which value remains constant while these requests are processed?', '100', ['capacity','storage capacity','100 MB']],
    'The scenario fixes capacity while requests alter the amount used.'),
  p({'1.1.14':'b'},
    ['The record also stores seat colour. State the field that can be omitted from this capacity decision.', 'seat colour', ['colour','color','seat color']],
    ['The record also stores the uploader’s display name. State the field that can be omitted from this capacity decision.', 'display name', ['uploader name','name','uploader’s display name']],
    'The omitted field does not affect whether enough capacity remains.'),
  p({'1.1.15':'e,f'},
    pick('Which action must be repeated for each request?', ['Compare current use plus the request with capacity','Reset current use to zero','Increase capacity by the requested amount'], 0),
    pick('Which action must be repeated for each upload?', ['Accept it if any space remains','Compare current use plus the upload size with capacity','Reset used storage to zero'], 1),
    'Each decision uses the current state, so an earlier acceptance can affect a later request.')
], [
  'Separate the capacity limit from the amount already used. The result part asks for the decision value; the stored-quantity part asks which item changes.',
  'Keep all sizes in MB and include space already used. The result part asks for the decision value; the stored-quantity part asks which item changes.'
]), [
  'A room has a fixed capacity of 40 seats. There are 37 booked seats. A booking is accepted only if the total would not exceed capacity. Answer each part for this initial state.',
  'An account has a fixed capacity of 100 MB. It currently uses 85 MB. An upload is accepted only if the total would not exceed capacity. Answer each part for this initial state.'
]),
q(127, 'CA1.1', 'Checking a reused solution', [
  p({'1.1.12':'a,b','1.1.11':'a'},
    ['A room allocator and an equipment allocator both reject requests when requested quantity exceeds available quantity. State the common comparison operator in requested ___ available that detects this failure.', '>', [], {kind:'code'}],
    ['A username rule and a product-code rule both reject text shorter than a minimum. State the common comparison operator in len(text) ___ minimum that detects this failure.', '<', [], {kind:'code'}],
    'Compare the rules themselves to identify the part that can be reused.'),
  p({'1.1.5':'c'},
    pick('Both allocation checks now call one shared routine. What benefit does this bring when their shared rule changes?', ['A separate rule must still be edited in every caller','The shared rule needs changing in one implementation','Testing the shared routine removes the need to test its callers'], 1),
    pick('Both length checks now call one shared routine. What benefit does this bring when their shared rule changes?', ['The shared rule needs changing in one implementation','All callers must now use the same maximum length','Each caller must still contain its own copy of the rule'], 0),
    'A genuine common rule can have one maintained implementation.'),
  p({'1.1.5':'d'},
    ['A room can hold multiple people, but a laptop can be lent to only one person. State the assumption that must be checked before reusing the room allocator: identical data storage or identical allocation rules?', 'identical allocation rules', ['allocation rules','identical rules']],
    ['Usernames ignore letter case, but product codes distinguish it. State the assumption that must be checked before reusing the username lookup: identical text lengths or identical comparison rules?', 'identical comparison rules', ['comparison rules','identical rules']],
    'Similar appearance or structure does not prove that all underlying rules are the same.'),
  p({'1.1.14':'c','1.1.13':'a'},
    ['The caller uses allocate(request) without seeing its search steps. Name the abstraction task this illustrates.', 'hiding internal workings', ['hide internal workings','hiding implementation details','hide implementation details']],
    ['The caller uses validate(code) without seeing its individual checks. Name the abstraction task this illustrates.', 'hiding internal workings', ['hide internal workings','hiding implementation details','hide implementation details']],
    'The interface exposes the operation while concealing its implementation.'),
  p({'1.1.17':'a,b'},
    pick('Before recognising shared allocation rules, which step makes separate responsibilities available to compare?', ['Remove all differing requirements','Decompose the system into smaller tasks','Write the final user interface'], 1),
    pick('Before recognising shared validation rules, which step makes separate responsibilities available to compare?', ['Write a separate copy of every rule','Remove all differing requirements','Decompose the system into smaller tasks'], 2),
    'Decomposition exposes tasks; pattern recognition then compares their requirements.'),
  p({'1.1.5':'f'},
    pick('An allocator omits wheelchair access requirements to simplify its room model. Why can its result be wrong?', ['A necessary constraint has been removed','The model contains too many rooms','The interface uses a function call'], 0),
    pick('A code validator ignores letter case even though case distinguishes products. Why can its result be wrong?', ['The strings contain letters','A necessary distinction has been removed','The routine has more than one caller'], 1),
    'Abstraction must preserve every distinction needed to meet the stated requirements.')
], [
  'Compare the allocation rules before reusing their implementation. Check which omitted detail could change whether an allocation is valid.',
  'Compare the validation rules before reusing their implementation. Check whether two strings that look similar must still be treated differently.'
]),
q(128, 'CA1.2', 'Reviewing algorithm representations', [
  p({'1.2.2':'b'},
    ['A flowchart puts total = price * quantity inside a terminator. State the symbol name that should replace it.', 'process', ['process symbol','rectangle']],
    ['A flowchart puts count = count + 1 inside a terminator. State the symbol name that should replace it.', 'process', ['process symbol','rectangle']],
    'A calculation is an action, so it belongs in a process symbol; a terminator marks entry or exit.'),
  p({'1.2.2':'d,g'},
    ['A decision tests balance >= charge. State the label on the branch that rejects an unaffordable payment.', 'No', ['False']],
    ['A decision tests used + size <= capacity. State the label on the branch that rejects an oversized upload.', 'No', ['False']],
    'The rejection branch is taken when the displayed condition is false.'),
  p({'1.2.2':'f'},
    ['An arrow leaves a process and enters a decision. Which is executed first: the process or the decision?', 'process'],
    ['An arrow leaves a decision’s Yes branch and enters an output. Which is executed first: the decision or the output?', 'decision'],
    'Follow the direction of the arrow to determine the execution order.'),
  p({'1.2.2':'h'},
    ['A written algorithm numbers a task 2 and its substeps 2.1 and 2.2. Give the number of the next substep at the same level.', '2.3'],
    ['A written algorithm numbers a task 4 and its substeps 4.1 and 4.2. Give the number of the next substep at the same level.', '4.3'],
    'The prefix identifies the parent task; the final number orders its substeps.'),
  p({'1.2.3':'b'},
    pick('A large flowchart shows all branches but repeated crossings obscure their routes. Choose the most relevant limitation.', ['No decision can be represented','Visual complexity can make control flow hard to follow','It must use Python variable names'], 1),
    pick('A flowchart makes a small loop easy to follow, but adding many nested decisions makes it span several pages. Choose the most relevant limitation.', ['The diagram becomes difficult to navigate and maintain','Arrows can only represent one execution','Written conditions cannot appear in decisions'], 0),
    'Flowcharts expose routes visually, but a complex diagram can obscure that benefit.'),
  p({'1.2.4':'a','1.2.5':'b'},
    pick('A client can read plain-language steps but cannot interpret Python operators. Which representation best supports their initial review?', ['Python source alone','A concise written description','A list of variable declarations alone'], 1),
    pick('A process owner can check ordered business instructions but does not know Python syntax. Which representation best supports their initial review?', ['A concise written description','Python source alone','A list of import statements alone'], 0),
    'A written description can communicate the process without requiring knowledge of a programming language.')
], 'Follow each connection or numbering level before deciding the next step. The replacement-symbol part asks for its technical name; a geometric shape is a different description.'),
context(q(129, 'CA1.2', 'Trace and repair a running calculation', [
  p({'1.2.7':'a'},
    ['State what the algorithm calculates.', 'sum of positive values', ['total of positive values','sum of positive numbers','total of positive numbers','positive total']],
    ['State what the algorithm calculates.', 'number of negative values', ['count of negative values','number of negative numbers','count of negative numbers','negative count']],
    'The condition selects qualifying values; the update determines whether the result is a sum or a count.'),
  p({'1.2.8':'a'},
    ['State the displayed result.', '7'], ['State the displayed result.', '2'],
    'Trace every item, keeping the running value only when the condition is true.', {kind:'number'}),
  p({'1.2.7':'b','1.2.6':'b'},
    ['How many times is the assignment inside the if executed?', '2'],
    ['How many times is the assignment inside the if executed?', '2'],
    'The conditional assignment runs only for values satisfying the comparison.', {kind:'number'}),
  p({'1.2.6':'a,c'},
    pick('Why is the initial assignment placed before the loop?', ['It resets the result before every item','It establishes a starting value that later iterations build on','It sorts the input values'], 1),
    pick('Why is the initial assignment placed before the loop?', ['It establishes a starting value that later iterations build on','It skips values failing the condition','It changes negative inputs to zero'], 0),
    'Initialisation happens once; the running value persists across iterations.'),
  p({'1.2.9':'a'},
    ['Which assignment would reset the running total incorrectly if it were moved inside the for loop?', 'total = 0', [], {kind:'code'}],
    ['Which assignment would reset the running count incorrectly if it were moved inside the for loop?', 'count = 0', [], {kind:'code'}],
    'Putting the initial zero assignment inside the loop would discard the accumulated result on every iteration.'),
  p({'1.2.10':'a'},
    ['Change the comparison operator so the algorithm includes zero as well as positive values.', '>=', [], {kind:'code'}],
    ['Change the comparison operator so the algorithm includes zero as well as negative values.', '<=', [], {kind:'code'}],
    'An inclusive comparison admits equality without admitting values on the other side of zero.')
], [
  'Keep a running total after each item. The assignment part asks for a code statement, rather than the final numeric result.',
  'Keep a running count after each item. Adding one records a qualifying item, whereas adding its value would calculate a different result.'
]), ['Use the complete algorithm below.','Use the complete algorithm below.'], [
  'values = [4, -2, 3, 0]\ntotal = 0\nfor value in values:\n    if value > 0:\n        total += value\nprint(total)',
  'values = [-4, 2, -3, 0]\ncount = 0\nfor value in values:\n    if value < 0:\n        count += 1\nprint(count)'
]),
q(130, 'CA1.3', 'Choose a development approach', [
  p({'1.3.1':'a'},
    ['A team has agreed what a new booking system must do, but has no existing components. Name the approach that starts with the complete system goal.', 'top-down', ['top down']],
    ['A team has agreed what a new reporting system must do, but has no existing components. Name the approach that starts with the complete system goal.', 'top-down', ['top down']],
    'Top-down design starts from the overall requirements and refines them into smaller tasks.'),
  p({'1.3.2':'a'},
    pick('Choose the benefit of this approach for the team in part a.', ['Existing components dictate the requirements','The overall requirements guide the smaller tasks','No interfaces need designing'], 1),
    pick('Choose the benefit of this approach for the team in part a.', ['The overall requirements guide the smaller tasks','Every low-level tool is already tested','All modules must use identical inputs'], 0),
    'Keeping the system goal in view helps the decomposition address the required behaviour.', {dependsOn:0}),
  p({'1.3.1':'b'},
    ['Another team must construct its solution from a fixed set of tested device-control components. Name the approach that starts from those components.', 'bottom-up', ['bottom up']],
    ['Another team must construct its solution from a fixed set of tested data-processing components. Name the approach that starts from those components.', 'bottom-up', ['bottom up']],
    'Bottom-up design assembles available building blocks into a larger solution.'),
  p({'1.3.2':'d'},
    pick('The assembled components do not provide a required audit trail. What limitation of the approach in part c does this reveal?', ['Available building blocks may leave a system requirement unmet','Working components always combine into a complete solution','The requirements should be limited to existing component features'], 0),
    pick('The assembled components cannot produce a required export. What limitation of the approach in part c does this reveal?', ['The requirements should be limited to existing component features','Available building blocks may leave a system requirement unmet','Passing isolated tests establishes that the complete system is suitable'], 1),
    'Component availability must be checked against the complete system requirements.', {dependsOn:2}),
  p({'1.3.1':'c','1.3.2':'e'},
    ['A tax calculation is isolated behind an unchanged interface. Name the approach that lets it be replaced without rewriting its callers.', 'modularisation', ['modularization','modular design']],
    ['A file exporter is isolated behind an unchanged interface. Name the approach that lets it be replaced without rewriting its callers.', 'modularisation', ['modularization','modular design']],
    'A module separates responsibility and lets callers depend on its interface.'),
  p({'1.3.2':'f','1.3.6':'a,b'},
    pick('Two modules both work separately, but disagree about the date format. What should the team address before choosing to combine them?', ['Their internal variable naming','Their interface contract','Their individual execution times'], 1),
    pick('Two modules both work separately, but one sends kilograms where the other expects grams. What should the team address before choosing to combine them?', ['Their interface contract','Their individual memory use','Their internal variable naming'], 0),
    'Combining modules needs agreement on transferred data, including its format and meaning.')
], 'Judge each approach against the starting point: overall requirements or available components. For linked reasons, connect the benefit or limitation to the preceding choice.'),
q(131, 'CA1.3', 'Investigating a recurring failure', [
  p({'1.3.3':'a'},
    pick('A service is restarted after every crash, but the same fault returns. Which investigation best addresses the recurring problem?', ['Record how quickly each restart finishes','Trace the failure to its underlying cause','Schedule more frequent restarts'], 1),
    pick('A nightly import fails repeatedly; rerunning it temporarily clears the queue. Which investigation best addresses the recurring problem?', ['Trace the failure to its underlying cause','Measure how quickly staff can rerun it','Increase the number of scheduled reruns'], 0),
    'Removing the cause can prevent recurrence; repeating a workaround only addresses the symptom.'),
  p({'1.3.4':'a'},
    ['A five-whys investigation finds that a crash follows exhausted memory. State what the next why-question should investigate.', 'why memory was exhausted', ['why was memory exhausted','why was memory exhausted?','why memory ran out','why did memory run out','why did memory run out?','cause of memory exhaustion']],
    ['A five-whys investigation finds that an import failed because its file was missing. State what the next why-question should investigate.', 'why the file was missing', ['why was the file missing','why was the file missing?','why the file is missing','cause of the missing file']],
    'Follow the cause just established, using evidence to investigate the next link.'),
  p({'1.3.5':'c'},
    pick('Logs have been collected. Which action analyses that evidence?', ['Store an unchanged copy of every log','Compare failing and successful runs to find a shared condition','Ask users to send the logs again'], 1),
    pick('Failure times have been collected. Which action analyses that evidence?', ['Compare failure times with scheduled file moves','Record the same times in another file','Ask users to repeat the same report'], 0),
    'Analysis interprets gathered evidence to find relationships.'),
  p({'1.3.4':'d'},
    ['The cause has been verified. Which follow-up action preserves the evidence and diagnosis for future incidents?', 'log', ['logging','log the findings','record the findings']],
    ['The failing sequence has been reproduced. Which follow-up action preserves the steps and diagnosis for future incidents?', 'log', ['logging','log the findings','record the findings']],
    'Logging retains the findings so another investigator can use them.'),
  p({'1.3.4':'f'},
    ['The verified cause is inside supplier software that the team cannot modify. State the appropriate follow-up action.', 'escalate', ['escalation','escalate to the supplier','contact the supplier']],
    ['The verified cause requires a security permission beyond the team’s authority. State the appropriate follow-up action.', 'escalate', ['escalation','escalate to an appropriate manager','escalate to a specialist']],
    'Escalate the evidence to someone with the expertise or authority to resolve the cause.'),
  p({'1.3.5':'f','1.3.4':'e'},
    pick('A fix has been deployed. Which evidence best supports closing the incident?', ['The original failing case now passes and relevant checks show the requirement is met','The source file has a more recent timestamp','The developer reports that the changed line now runs'], 0),
    pick('A fix has been deployed. Which evidence best supports closing the incident?', ['The application opens without an immediate error','The original failing case now passes and relevant checks show the requirement is met','The support ticket has been assigned to a new owner'], 1),
    'Review against the original problem before closing the incident.')
], 'Follow the evidence from symptom to cause. In the five-whys part, continue from the cause already found; after a fix, return to the original failing case.')
];
