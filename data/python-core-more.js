import {question as q, part as p, number as n, choice as c} from './helpers.js';
const token=(prompt,answer,extra={})=>p(prompt,answer,{kind:'code',...extra});
export default [
 q(54,'arrays','Read a typed array','Predict the output',['CA2.3.1','CA2.3.2','arrays','data types'],i=>{const a=i+3;return {prompt:'The standard-library array stores signed integers using type code i. Predict values and identify its element-type restriction.',code:`from array import array\nreadings = array("i", [${a}, ${a+2}, ${a+4}])\nreadings[1] += 1\nprint(readings[0], readings[1], readings[-1])`,hint:'An array index selects one element; all elements must fit the selected type.',parts:[n('First output',a),n('Second output',a+3),n('Third output',a+4),n('Number of elements',3),c('Can readings.append("hot") add text to this integer array?','No',['Yes','No']),c('What does the type code i specify?','Signed integer elements',['Signed integer elements','The initial array length','The input file name'])]};}),
 q(55,'arrays','Repair an array index','Fix the code',['CA2.3.4','arrays','indexing'],i=>{const a=i+5;return {prompt:'The code should replace the final array item with zero. Fix the out-of-range index and trace the repaired result.',code:`from array import array\ncounts = array("i", [${a}, ${a+1}, ${a+2}])\ncounts[len(counts)] = 0`,hint:'The last valid index is one below the length.',parts:[token('Replacement index expression','len(counts) - 1'),n('Corrected counts[0]',a),n('Corrected counts[1]',a+1),n('Corrected counts[2]',0),c('Original exception','IndexError',['TypeError','IndexError','ValueError']),c('Does replacing an element change the array length?','No',['Yes','No'])]};}),
 q(56,'arrays','Append a converted reading','Complete the code',['CA2.3.3','CA2.2.9','arrays','conversion'],i=>{const a=i+4;return {prompt:'Convert the text reading to an integer, append it, then predict the resulting array.',code:`from array import array\nreadings = array("i", [2, 3])\nraw = "${a}"\nreadings.append(___)`,hint:'The text must become a compatible numeric value before insertion.',parts:[token('Expression for ___','int(raw)'),n('New length',3),n('New final value',a),n('Sum of readings',a+5),c('Can append assign a fourth item by index 3 before it exists?','No',['Yes','No']),c('Which alternative text would int reject?','four',['4',' 4 ','four'])]};}),
 q(57,'selection','Dispatch a match case','Predict the output',['CA2.6.2','CA2.6.6','match/case'],i=>{const x=i+2;return {prompt:'Predict each return value. The final wildcard case is a fallback.',code:`def route(code):\n    match code:\n        case ${x}:\n            return "save"\n        case ${x+1}:\n            return "load"\n        case _:\n            return "help"`,hint:'Cases are checked in order; _ matches any remaining value.',parts:[p(`route(${x})`,'save'),p(`route(${x+1})`,'load'),p('route(99)','help'),p('route(-1)','help'),n('Number of parameters',1),c('Meaning of _ here','Wildcard pattern',['Wildcard pattern','Undefined variable error','An empty string'])]};}),
 q(58,'selection','Repair an inclusive case guard','Fix the code',['CA2.6.8','CA2.4.7','match/case'],i=>{const limit=i+10;return {prompt:`Values of at least ${limit} must return "high". Repair the strict comparison in the guard, then trace the corrected function.`,code:`def band(value):\n    match value:\n        case number if number > ${limit}:\n            return "high"\n        case _:\n            return "low"`,hint:'An inclusive lower bound accepts the boundary itself.',parts:[token('Replacement for >','>='),p(`band(${limit-1})`,'low'),p(`band(${limit})`,'high'),p(`band(${limit+1})`,'high'),p('band(0)','low'),c('Which test exposes the original error?',String(limit),[String(limit-1),String(limit),String(limit+1)])]};}),
 q(59,'selection','Complete a match fallback','Complete the code',['CA2.6.7','match/case','syntax'],i=>{const x=i+1;return {prompt:'Handle the two listed codes explicitly, then return "unknown" for every other code. Complete the wildcard pattern.',code:`def label(code):\n    match code:\n        case ${x}:\n            return "first"\n        case ${x+1}:\n            return "second"\n        case ___:\n            return "unknown"`,hint:'Use the wildcard symbol, not a quoted string.',parts:[token('Pattern for ___','_'),p(`label(${x})`,'first'),p(`label(${x+1})`,'second'),p('label(90)','unknown'),p('label(-5)','unknown'),c('Why must an unconditional wildcard be last?','It matches every remaining value',['It matches every remaining value','It matches only strings','It always raises an exception'])]};})
];
