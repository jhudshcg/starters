import {examQuestion as q, item as p} from './exam-helpers.js';
const pick = (prompt, options, correct) => [prompt, options[correct], [], {options}];

export default [
q(144, 'CA2.12', 'What early and isolated tests reveal', [
  p({'2.12.2.1':'b,c'},
    pick('A clickable booking prototype shows users cannot understand the proposed workflow. What decision can concept testing support now?', ['Revise the idea before building the full system','Approve the production system’s response time','Approve the workflow because the prototype opens'], 0),
    pick('A paper mock-up shows that the proposed stock-entry process misses an essential task. What decision can concept testing support now?', ['Certify the finished software','Revise the idea before building the full system','Approve the proposal because every existing mock-up screen was viewed'], 1),
    'Testing the concept can expose an unsuitable proposal before full implementation costs are incurred.'),
  p({'2.12.2.1':'d'},
    pick('Users can follow a paper prototype. Which claim still needs evidence from implemented software?', ['The proposal can be discussed','The production service meets its response-time requirement','Users can identify the proposed screens'], 1),
    pick('Users can follow a clickable prototype with fixed example data. Which claim still needs evidence from implemented software?', ['The proposed navigation can be tried','The prototype can demonstrate the idea','The real service handles simultaneous database updates correctly'], 2),
    'A simplified prototype does not establish the behaviour of implementation details it omits.'),
  p({'2.12.2.1':'f,g'},
    pick('An isolated calculate_fee test returns the wrong amount before any payment call. Where should investigation start?', ['The fee routine and its test setup','The payment network alone','The dispatch printer alone'], 0),
    pick('An isolated count_valid test returns the wrong count before any database write. Where should investigation start?', ['The report printer alone','The counting routine and its test setup','The database connection alone'], 1),
    'Testing a unit independently narrows the initial investigation to that unit and its test setup.'),
  p({'2.12.2.1':'h'},
    pick('All pricing-unit tests pass. Which further fault could still appear when pricing is connected to payment?', ['The same isolated price calculation now necessarily produces a different result','Payment interprets pounds as pence','The price routine must accept every payment response format'], 1),
    pick('All date-parser unit tests pass. Which further fault could still appear when it is connected to booking?', ['Booking sends a different date format from the agreed one','The parser’s isolated tests guarantee the caller uses its interface correctly','The same standalone parser test must now fail'], 0),
    'Isolated success does not prove that collaborating components agree on their interface.'),
  p({'2.12.2.1':'n,o'},
    pick('Pricing sends 12.50 pounds and payment charges 12.50 pence. Which integration test directly targets this defect?', ['Time a single multiplication','Check that the amount retains its meaning across the interface','Check whether the payment module accepts any numeric value'], 1),
    pick('A sender supplies a date as day/month/year and the receiver reads it as month/day/year. Which integration test directly targets this defect?', ['Check that the date retains its meaning across the interface','Check whether the sender opens','Check the number of comments in the receiver'], 0),
    'Integration tests check that the connected parts exchange data with the agreed format and meaning.'),
  p({'2.12.2.1':'p'},
    pick('The connected booking and payment test fails, although both units passed alone. Why can diagnosis take more work?', ['The failure can involve either component or their interaction','Unit tests must be deleted before investigation','Passing unit tests prove the fault must be in the test data'], 0),
    pick('The connected importer and database test fails, although both units passed alone. Why can diagnosis take more work?', ['Passing component tests prove the database alone is responsible','The failure can involve either component or their interaction','The expected result is no longer needed'], 1),
    'A combined failure has more possible locations than an isolated unit failure.'),
  p({'2.12.1.1':'a,d'},
    pick('Why retain isolated unit results when investigating the failed connection?', ['They prove the complete product is acceptable','They provide evidence separating component behaviour from interface behaviour','They replace all integration tests'], 1),
    pick('Why retain isolated component results when investigating the failed import connection?', ['They provide evidence separating component behaviour from interface behaviour','They prove all production records are valid','They replace all end-to-end tests'], 0),
    'Separate checks help distinguish faults within components from faults in their interaction.')
], 'Read each test result as evidence with a limited scope. A unit that passes alone may still be given the wrong format or units when connected to another component.'),
q(145, 'CA2.12', 'Requirements, users and acceptance', [
  p({'2.12.2.1':'v,w'},
    pick('Stock, payment and dispatch pass separate tests. Which system test checks the complete ordering requirement?', ['Run an order from submission through payment to dispatch confirmation','Retest only the price multiplication','Check only the dispatch button label'], 0),
    pick('Import, calculation and export pass separate tests. Which system test checks the complete reporting requirement?', ['Retest only the total function','Check only the export filename','Import the source, generate the report and verify its exported content'], 2),
    'A system test checks behaviour of the complete integrated solution against its requirements.'),
  p({'2.12.2.1':'x'},
    ['A realistic system test needs separate servers, devices and representative records. State the resource cost this increases: test-environment cost or source-code line count.', 'test-environment cost', ['test environment cost','environment cost','cost']],
    ['A realistic system test needs a full service installation, connected peripherals and representative users. State the resource cost this increases: test-environment cost or number of variable names.', 'test-environment cost', ['test environment cost','environment cost','cost']],
    'Maintaining a realistic complete test environment requires resources.'),
  p({'2.12.2.1':'z,aa'},
    pick('A delivered booking system passes technical tests, but the client cannot perform an agreed refund task. Which acceptance decision is supported?', ['Withhold acceptance until the agreed business requirement is met','Accept because the code runs without crashing','Remove the requirement from the test record'], 0),
    pick('A delivered reporting system passes technical tests, but the client cannot produce an agreed monthly export. Which acceptance decision is supported?', ['Accept because all screens open','Withhold acceptance until the agreed business requirement is met','Replace the expected result with the observed one'], 1),
    'Acceptance testing gives the customer evidence about agreed business needs, not just technical operation.'),
  p({'2.12.2.1':'ab'},
    pick('The client accepts after trying three ordinary workflows. Which conclusion is justified?', ['Every internal branch has been exercised','Those workflows met the checked criteria; untested faults may remain','All future user behaviour is covered'], 1),
    pick('The client accepts after checking the agreed demonstration scenarios. Which conclusion is justified?', ['No security fault can remain','All unusual inputs have been checked','Those scenarios met the checked criteria; untested faults may remain'], 2),
    'Acceptance evidence is limited by the criteria and scenarios actually tested.'),
  p({'2.12.2.1':'ad,ae'},
    ['Five intended users can place an order, but four repeatedly open the wrong menu first. Which should a usability review investigate: navigation or numerical precision?', 'navigation'],
    ['Five intended users can save a report, but four cannot find the save control without help. Which should a usability review investigate: discoverability of controls or arithmetic speed?', 'discoverability of controls', ['discoverability','controls','navigation']],
    'Observed difficulty can expose an interaction problem even when the eventual output is correct.'),
  p({'2.12.2.1':'af'},
    pick('Only the developers try a service intended for first-time public users. What limits the usability evidence?', ['The participants are not representative of the intended users','Experienced users cannot complete tasks','Usability can only be measured after release'], 0),
    pick('Only confident desktop users try a service intended mainly for mobile novices. What limits the usability evidence?', ['Every participant must own the same phone','The participants are not representative of the intended users','The test needs no observations'], 1),
    'A sample unlike the intended users may conceal real usability difficulties.'),
  p({'2.12.1.1':'e'},
    pick('A booking service works in component tests. Why run the complete customer journey before public launch?', ['To check that the resulting service delivers the required end-to-end outcome','To replace the customer’s agreed requirements','To guarantee there will never be another fault'], 0),
    pick('A reporting service works in component tests. Why run the complete user journey before release?', ['To prove that every possible input was tested','To check that the resulting service delivers the required end-to-end outcome','To avoid recording any actual results'], 1),
    'The delivered service must work as a whole, not merely as separate components.')
], 'Distinguish a correct output from an easy-to-use process and from a product that meets agreed business needs. Judge claims against the users and scenarios actually tested.'),
q(146, 'CA2.12', 'Interpreting workload and regression evidence', [
  p({'2.12.2.1':'r,s'},
    ['A requirement limits search response time to 2 seconds. Tests record 0.8, 1.1 and 2.4 seconds. State the recorded value that breaches the limit.', '2.4'],
    ['A requirement limits report response time to 3 seconds. Tests record 1.2, 3.6 and 2.9 seconds. State the recorded value that breaches the limit.', '3.6'],
    'Compare the measured response times with the required maximum.', {kind:'number'}),
  p({'2.12.2.1':'t'},
    pick('The timing test used ten records, but production will contain a million. Why is the passing result insufficient?', ['The test workload may not represent production behaviour','Small datasets cannot produce timings','Every larger dataset must take exactly twice as long'], 0),
    pick('The timing test used one local user, but production will have hundreds of remote users. Why is the passing result insufficient?', ['A local test proves remote performance','Remote users cannot be simulated','The test conditions may not represent production behaviour'], 2),
    'Performance evidence depends on realistic data, workload and environment.'),
  p({'2.12.2.1':'al,am'},
    ['A service must respond within 2 seconds. Tests with 100, 200 and 300 simultaneous users take 0.5, 1.8 and 4 seconds respectively. State the largest tested workload that meets the limit.', '200'],
    ['A service must respond within 3 seconds. Tests with 250, 500 and 750 simultaneous users take 1, 2.6 and 5 seconds respectively. State the largest tested workload that meets the limit.', '500'],
    'Compare the measured response time at each tested workload with the requirement; this does not establish the untested exact capacity limit.', {kind:'number'}),
  p({'2.12.2.1':'ak,al,am'},
    ['Demand is then increased beyond the stated limit to observe failure and recovery. Name this form of testing.', 'stress testing', ['stress']],
    ['Demand is then raised above the required capacity to observe degradation and recovery. Name this form of testing.', 'stress testing', ['stress']],
    'Stress testing examines behaviour beyond normal operating limits.'),
  p({'2.12.2.1':'an'},
    pick('The load generator can only simulate 20 users reliably, but the target is 200. What limitation must be addressed?', ['The generator lacks sufficient capacity for the required workload','The requirement must already have passed','The application cannot have a response-time requirement'], 0),
    pick('The stress-test environment runs out of network capacity before the service does. What limitation must be addressed?', ['The service must have unlimited capacity','The test infrastructure is limiting the generated workload','The expected results are unnecessary'], 1),
    'The test infrastructure must generate and measure the intended demand without becoming the limiting factor.'),
  p({'2.12.2.1':'ah,ai'},
    ['After changing tax calculations, a previously passing delivery-charge test fails. Name the testing method that exposed this unintended effect.', 'regression testing', ['regression']],
    ['After changing the date parser, a previously passing export test fails. Name the testing method that exposed this unintended effect.', 'regression testing', ['regression']],
    'Regression tests rerun previously working cases to expose unintended effects of a change.'),
  p({'2.12.2.1':'aj'},
    pick('An agreed requirement changes, but the regression suite retains the old expected result. What maintenance is needed?', ['Delete all tests that fail','Review and update the affected expectations against the new requirement','Always replace expected results with actual outputs'], 1),
    pick('A workflow is deliberately redesigned, but the regression suite still follows its old steps. What maintenance is needed?', ['Stop testing unchanged features','Review and update affected cases against the agreed workflow','Treat every failure as a product defect'], 1),
    'Regression suites need maintained inputs and expectations; automatic replacement with actual output would hide defects.')
], 'Compare measurements with the stated requirement and the workload that produced them. Expected-load testing and deliberately exceeding capacity answer different questions.'),
q(147, 'CA2.12', 'Choosing test evidence from rules and code', [
  p({'2.12.2.1':'ap,aq'},
    pick('Two teams test different implementations of the same specified fee rule without seeing their source. What should their expected results come from?', ['Each implementation’s current output','The shared fee specification','The number of source-code lines'], 1),
    pick('Two teams test different implementations of the same specified eligibility rule without seeing their source. What should their expected results come from?', ['The shared eligibility specification','The order of internal branches','Each implementation’s current output'], 0),
    'Closed-box tests can check the same externally required behaviour independently of implementation.'),
  p({'2.12.2.1':'ar'},
    pick('All planned closed-box cases pass. What can the tester still not infer without further evidence?', ['Every internal branch was executed','The tested outputs matched expectations','The chosen inputs were supplied'], 0),
    pick('All planned input/output tests pass. What can the tester still not infer without further evidence?', ['The tests ran with recorded inputs','Every internal path was exercised','The observed outputs matched the expected outputs'], 1),
    'Externally chosen test cases can leave internal paths unvisited.'),
  p({'2.12.2.1':'at,au'},
    ['Open-box inspection finds if score >= 10: with an untested True branch. Give the smallest integer score that exercises that branch.', '10'],
    ['Open-box inspection finds if count > 4: with an untested True branch. Give the smallest integer count that exercises that branch.', '5'],
    'Knowledge of the actual comparison identifies an input that visits the missing branch.', {kind:'number'}),
  p({'2.12.2.1':'av'},
    ['A tester has an executable and specification but cannot inspect the source. State the missing information needed to plan code-path-based open-box tests.', 'internal code structure', ['source code','code','internal structure','implementation','program structure','code paths']],
    ['A tester knows the required outputs but cannot inspect the implemented decisions. State the missing information needed to plan code-path-based open-box tests.', 'internal code structure', ['source code','code','internal structure','implementation','program structure','code paths']],
    'Open-box test design depends on knowledge of the implementation.'),
  p({'2.12.2.1':'j,k'},
    ['The requirement accepts ages 16–19 inclusive, but code uses age > 16. Give the accepted boundary value that reveals this defect.', '16'],
    ['The requirement accepts quantities 1–10 inclusive, but code uses quantity < 10. Give the accepted boundary value that reveals this defect.', '10'],
    'The incorrect strict comparison rejects a value that should be accepted at the limit.', {kind:'number'}),
  p({'2.12.2.1':'l'},
    pick('Both endpoints pass, but a special branch incorrectly rejects the interior value 17. What does this show about testing only boundaries?', ['It may miss faults affecting interior values','It proves every input is accepted','It is the same as testing every code path'], 0),
    pick('Both endpoints pass, but a special branch incorrectly rejects the interior value 6. What does this show about testing only boundaries?', ['It guarantees the range check is fully correct','It may miss faults affecting interior values','It proves the program has no branches'], 1),
    'Boundary cases target limit defects, not every possible fault inside the allowed range.'),
  p({'2.12.4.2':'a'},
    ['A field accepts integers 4–9 inclusive. Give the three consecutive values centred on the upper boundary, in ascending order, separated by commas.', '8, 9, 10', ['8,9,10','[8,9,10]','[8, 9, 10]']],
    ['A field accepts integers 3–7 inclusive. Give the three consecutive values centred on the lower boundary, in ascending order, separated by commas.', '2, 3, 4', ['2,3,4','[2,3,4]','[2, 3, 4]']],
    'Choose the limit itself and its immediately adjacent values to test the transition.')
], 'Derive expected behaviour from the requirement, then use the code to see which path a value takes. A boundary can be valid; include the neighbouring invalid value when testing the transition.'),
q(148, 'CA2.12', 'Component faults and reliable automation', [
  p({'2.12.1.1':'b'},
    pick('A scanner sometimes reads the wrong digits even when disconnected from the application. Why test the device separately?', ['To establish whether the hardware already introduces the fault','To prove the database lookup is correct','To replace every software test'], 0),
    pick('A printer omits characters on its own diagnostic page. Why test the device separately?', ['To prove the application sent invalid data','To establish whether the hardware already introduces the fault','To skip checking its output'], 1),
    'An independent device test helps distinguish hardware faults from application or integration faults.'),
  p({'2.12.1.1':'c'},
    ['An import fails on a customer record with no required ID. Which should be checked before import: source data or output-file permissions?', 'source data', ['data','input data','records']],
    ['A report fails on a source record with a price stored as "unknown". Which should be checked before processing: source data or output-directory permissions?', 'source data', ['data','input data','records']],
    'Checking source records can expose missing or malformed values before downstream components depend on them.'),
  p({'2.12.3.1':'a'},
    ['A tester records a fixed sequence of menu clicks and replays it each day. Name this automation method.', 'macro', ['macros']],
    ['A tester records a fixed sequence of application actions and replays it for each build. Name this automation method.', 'macro', ['macros']],
    'A macro records or stores a sequence of application actions for replay.'),
  p({'2.12.3.1':'a'},
    pick('A replayed macro clicks the wrong control after the layout changes. What should happen before trusting its results?', ['Review the recorded actions against the changed interface','Treat every output as a correct test result','Increase the number of replays without checking the actions'], 0),
    pick('A replayed macro opens the wrong menu after navigation changes. What should happen before trusting its results?', ['Use the old expected result regardless of the action','Review the recorded actions against the changed interface','Stop recording which actions ran'], 1),
    'Replay is useful for stable repeated actions, but changed controls require the macro to be checked.'),
  p({'2.12.3.1':'b'},
    ['A tester needs a written sequence of commands to submit 100 different input files and collect exit statuses. Name the automation method.', 'script', ['scripts','test script','scripting']],
    ['A tester needs a written sequence of commands to supply 100 data values and collect returned outputs. Name the automation method.', 'script', ['scripts','test script','scripting']],
    'A script can iterate over data and collect results without repeating the commands manually.'),
  p({'2.12.3.1':'c'},
    pick('A functional testing tool reports that every button was clicked. What must also be checked to establish that the workflow passed?', ['The resulting outputs match the expected outcomes','The automation process exited without a tool error','The same number of actions ran as in the previous test'], 0),
    pick('A functional testing tool reports that every request was submitted. What must also be checked to establish that the workflow passed?', ['Every request had a different identifier','The resulting outputs match the expected outcomes','The request count matches the number of planned steps'], 1),
    'Executing actions alone is not a pass: the tool must check their outcomes against expectations.'),
  p({'2.12.4.3':'a'},
    pick('Before selecting values or writing automated steps, what should determine which tests are included?', ['The behaviours and requirements that need checking','Only the inputs the program currently accepts','Only whichever cases are quickest to automate'], 0),
    pick('Before collecting actual results or choosing a replay tool, what should determine which tests are included?', ['Only tests that passed the previous release','The behaviours and requirements that need checking','Only the commands the tester already remembers'], 1),
    'Identify the required checks before choosing their inputs and implementation.')
], 'A successful replay only shows that actions ran. Check both the source of a fault and the evidence needed to call the behaviour correct.')
];
